apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: kube-system
data:
  cluster-name: "helixflow-prod"
  region: "us-east-1"
  environment: "production"
  log-level: "info"
  gpu-enabled: "true"
  istio-enabled: "true"
  monitoring-enabled: "true"
  security-enabled: "true"